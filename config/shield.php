<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default path to scan
    |--------------------------------------------------------------------------
    */
    'path' => base_path(),

    /*
    |--------------------------------------------------------------------------
    | Excluded path segments (relative to scan path)
    |--------------------------------------------------------------------------
    */
    'exclude' => [
        'vendor',
        'node_modules',
        'storage',
        'bootstrap/cache',
        'tests',
        'fixtures',
    ],

    /*
    |--------------------------------------------------------------------------
    | File extensions to include for generic scans
    |--------------------------------------------------------------------------
    */
    'php_extensions' => ['php'],

    /*
    |--------------------------------------------------------------------------
    | Min severity to report (null = all)
    |--------------------------------------------------------------------------
    | critical | high | medium | low | info
    */
    'min_severity' => null,

    /*
    |--------------------------------------------------------------------------
    | Entropy-based secret detection (secrets scanner)
    |--------------------------------------------------------------------------
    */
    'entropy_threshold' => 4.5,

    /*
    |--------------------------------------------------------------------------
    | Max file size (bytes) for entropy analysis
    |--------------------------------------------------------------------------
    */
    'max_entropy_file_bytes' => 512000,

    /*
    |--------------------------------------------------------------------------
    | Monorepo: label => path relative to main app or absolute
    |--------------------------------------------------------------------------
    */
    'projects' => [
        // 'api' => base_path('../api'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Watch mode interval (seconds) for shield:scan --watch
    |--------------------------------------------------------------------------
    */
    'watch_interval' => 2.0,

];
